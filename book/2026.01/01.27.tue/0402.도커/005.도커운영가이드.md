https://drive.google.com/drive/folders/1o6K1IQ5JQ4u9BdyBfRNINgwZgjVqCJc_

커리큘럼 상 설명은 하지만 우린 거의 안 쓸 것

 Docker file과 Docker compose가 있음

 도커 파일은 도커 엔진이 연결해주는 유저와만 연결, 도커 컴포즈도 서버 벗어나면 연결 안 됨

 ##### 네트워크
 host: 버추얼박스의 host only는 도커허브 네트워크에서 유래함
 overlay: 쫙~ 다 통신 할 수 있는 것 : 쿠버네티스 깔 때 오버레이 함 () =>클러스터링 하려면 네트워크 오버레이 해줘야 함
 브릿지는 쫙 되는데 오버레이는 무작위로 할 수 있는 것

지동이   
 >브릿지 = 한 집 안에서 다 연결  
 >오버레이 = 여러 집을 하나의 가상 동네로 묶음

#### 정리 PRUNE 전략

```
$ docker image prune -a -f --filter"until=24h"
```

```
# 임시로 아이피 할당 한 것 회수하는데 모두 회수하지 않으면 못 사용함
$ docker network prune -f
```

#### 볼륨 생성과 마운트
볼륨 생성, 마운트: 어려울 것 없음
# stand
#### 볼륨 생ㅅㅇ 및 복구
